# Production Environment Feature Flags
# ====================================
# SAFE DEFAULTS - All flags default to false for maximum safety

# Semantic matching - DISABLED by default for safety
FEATURE_SEMANTIC_MATCH=false

# Debug output - DISABLED by default for production
FEATURE_DEBUG_OUTPUT=false

# Staging-specific features - DISABLED in production
FEATURE_STAGING_SEMANTIC=false

# Force traditional filtering (rollback mode) - DISABLED by default
FEATURE_FORCE_TRADITIONAL=false
