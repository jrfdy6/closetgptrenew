# Dedicated lightweight service. Same backend root/image dependencies; no GPU or
# image-provider credentials needed. Credentials stay server-side on Railway.
[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m src.worker.gamification_runner"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
