# 🚀 railway.mcp.toml — Dedicated config for MCP service
# Build from repo root, but use Dockerfile inside backend/

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile.mcp"
# 👇 Build context is repo root (not backend)
# Do NOT set sourceDir here or Railway will misresolve paths

[deploy]
startCommand = "pip install -r backend/requirements-mcp-minimal.txt && python3 backend/mcp_server.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

