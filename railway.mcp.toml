# HTTP Gateway for ChatGPT Apps SDK
# Proxies requests from ChatGPT to main ClosetGPT backend

[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile.gateway"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

