[build]
builder = "dockerfile"
sourceDir = "."

[deploy]
startCommand = "uvicorn src.app:app --host 0.0.0.0 --port 8080"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build.env]
NODE_ENV = "production"
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_PIP_VERSION = "23.3.1"
