[build]
builder = "nixpacks"
sourceDir = "backend"

[deploy]
startCommand = "python minimal_test_app.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build.env]
NODE_ENV = "production"
NIXPACKS_PYTHON_VERSION = "3.11"
NIXPACKS_PIP_VERSION = "23.3.1"
